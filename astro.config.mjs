// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// TODO: replace with the real GitHub username once known — root user
	// sites (username.github.io) need no `base`, just the correct `site`.
	site: 'https://REPLACE_WITH_USERNAME.github.io',
});
